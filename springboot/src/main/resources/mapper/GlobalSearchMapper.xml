<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.novelcraft.web.mapper.GlobalSearchMapper">

    <!-- ==================== 全局搜索 - 章节 ==================== -->

    <!--
        功能: 搜索章节（标题和内容）
        参数: projectId - 项目ID, keyword - 搜索关键词
        返回: List<SearchResultDTO> 搜索结果列表
        说明:
            - 精确匹配标题优先级最高(1)，模糊标题匹配(2)，模糊内容匹配(4)
            - type='chapter', typeLabel='📑 章节'
            - matchField标识匹配字段（title/content）
    -->
    <select id="searchChapters" resultType="com.novelcraft.web.model.SearchResultDTO">
        SELECT
          'chapter' as type,
          '📑 章节' as typeLabel,
          id,
          title,
          content as description,
          #{keyword} as keyword,
          CASE
            WHEN title = #{keyword} THEN 1
            WHEN title LIKE CONCAT('%', #{keyword}, '%') THEN 2
            WHEN content LIKE CONCAT('%', #{keyword}, '%') THEN 4
            ELSE 5
          END as priority,
          CASE
            WHEN title LIKE CONCAT('%', #{keyword}, '%') THEN 'title'
            WHEN content LIKE CONCAT('%', #{keyword}, '%') THEN 'content'
            ELSE 'content'
          END as matchField,
          create_time as createTime,
          update_time as updateTime
        FROM novel_chapter
        WHERE project_id = #{projectId}
          AND deleted = 0
          AND (title LIKE CONCAT('%', #{keyword}, '%')
               OR content LIKE CONCAT('%', #{keyword}, '%'))
    </select>

    <!-- ==================== 全局搜索 - 人物 ==================== -->

    <!--
        功能: 搜索人物（姓名、描述、性格）
        参数: projectId - 项目ID, keyword - 搜索关键词
        返回: List<SearchResultDTO> 搜索结果列表
        说明:
            - 精确匹配姓名优先级最高(1)，模糊姓名(2)，模糊描述/性格(4)
            - type='character', typeLabel='👤 人物'
            - 搜索name、description、personality三个字段
    -->
    <select id="searchCharacters" resultType="com.novelcraft.web.model.SearchResultDTO">
        SELECT
          'character' as type,
          '👤 人物' as typeLabel,
          id,
          name as title,
          description,
          #{keyword} as keyword,
          CASE
            WHEN name = #{keyword} THEN 1
            WHEN name LIKE CONCAT('%', #{keyword}, '%') THEN 2
            WHEN description LIKE CONCAT('%', #{keyword}, '%') THEN 4
            WHEN personality LIKE CONCAT('%', #{keyword}, '%') THEN 4
            ELSE 5
          END as priority,
          CASE
            WHEN name LIKE CONCAT('%', #{keyword}, '%') THEN 'name'
            WHEN description LIKE CONCAT('%', #{keyword}, '%') THEN 'description'
            WHEN personality LIKE CONCAT('%', #{keyword}, '%') THEN 'personality'
            ELSE 'description'
          END as matchField,
          create_time as createTime,
          update_time as updateTime
        FROM novel_character
        WHERE project_id = #{projectId}
          AND deleted = 0
          AND (name LIKE CONCAT('%', #{keyword}, '%')
               OR description LIKE CONCAT('%', #{keyword}, '%')
               OR personality LIKE CONCAT('%', #{keyword}, '%'))
    </select>

    <!-- ==================== 全局搜索 - 世界观 ==================== -->

    <!--
        功能: 搜索世界观设定（名称、内容）
        参数: projectId - 项目ID, keyword - 搜索关键词
        返回: List<SearchResultDTO> 搜索结果列表
        说明:
            - 精确匹配名称优先级最高(1)，模糊名称(2)，模糊内容(4)
            - type='world', typeLabel='🌍 世界观'
    -->
    <select id="searchWorldSettings" resultType="com.novelcraft.web.model.SearchResultDTO">
        SELECT
          'world' as type,
          '🌍 世界观' as typeLabel,
          id,
          name as title,
          content as description,
          #{keyword} as keyword,
          CASE
            WHEN name = #{keyword} THEN 1
            WHEN name LIKE CONCAT('%', #{keyword}, '%') THEN 2
            WHEN content LIKE CONCAT('%', #{keyword}, '%') THEN 4
            ELSE 5
          END as priority,
          CASE
            WHEN name LIKE CONCAT('%', #{keyword}, '%') THEN 'name'
            WHEN content LIKE CONCAT('%', #{keyword}, '%') THEN 'content'
            ELSE 'content'
          END as matchField,
          create_time as createTime,
          update_time as updateTime
        FROM novel_world_setting
        WHERE project_id = #{projectId}
          AND deleted = 0
          AND (name LIKE CONCAT('%', #{keyword}, '%')
               OR content LIKE CONCAT('%', #{keyword}, '%'))
    </select>

    <!-- ==================== 全局搜索 - 大纲 ==================== -->

    <!--
        功能: 搜索大纲节点（标题、描述）
        参数: projectId - 项目ID, keyword - 搜索关键词
        返回: List<SearchResultDTO> 搜索结果列表
        说明:
            - 精确匹配标题优先级最高(1)，模糊标题(2)
            - type='outline', typeLabel='📋 大纲'
    -->
    <select id="searchOutlines" resultType="com.novelcraft.web.model.SearchResultDTO">
        SELECT
          'outline' as type,
          '📋 大纲' as typeLabel,
          id,
          title,
          description,
          #{keyword} as keyword,
          CASE
            WHEN title = #{keyword} THEN 1
            WHEN title LIKE CONCAT('%', #{keyword}, '%') THEN 2
            WHEN description LIKE CONCAT('%', #{keyword}, '%') THEN 5
            ELSE 5
          END as priority,
          CASE
            WHEN title LIKE CONCAT('%', #{keyword}, '%') THEN 'title'
            WHEN description LIKE CONCAT('%', #{keyword}, '%') THEN 'description'
            ELSE 'description'
          END as matchField,
          create_time as createTime,
          update_time as updateTime
        FROM novel_outline
        WHERE project_id = #{projectId}
          AND deleted = 0
          AND (title LIKE CONCAT('%', #{keyword}, '%')
               OR description LIKE CONCAT('%', #{keyword}, '%'))
    </select>

    <!-- ==================== 全局搜索 - 情节线 ==================== -->

    <!--
        功能: 搜索情节线（名称、描述）
        参数: projectId - 项目ID, keyword - 搜索关键词
        返回: List<SearchResultDTO> 搜索结果列表
        说明:
            - 精确匹配名称优先级最高(1)，模糊名称(2)，模糊描述(4)
            - type='plot', typeLabel='🎯 情节'
    -->
    <select id="searchPlotThreads" resultType="com.novelcraft.web.model.SearchResultDTO">
        SELECT
          'plot' as type,
          '🎯 情节' as typeLabel,
          id,
          name as title,
          description,
          #{keyword} as keyword,
          CASE
            WHEN name = #{keyword} THEN 1
            WHEN name LIKE CONCAT('%', #{keyword}, '%') THEN 2
            WHEN description LIKE CONCAT('%', #{keyword}, '%') THEN 4
            ELSE 5
          END as priority,
          CASE
            WHEN name LIKE CONCAT('%', #{keyword}, '%') THEN 'name'
            WHEN description LIKE CONCAT('%', #{keyword}, '%') THEN 'description'
            ELSE 'description'
          END as matchField,
          create_time as createTime,
          update_time as updateTime
        FROM novel_plot_thread
        WHERE project_id = #{projectId}
          AND deleted = 0
          AND (name LIKE CONCAT('%', #{keyword}, '%')
               OR description LIKE CONCAT('%', #{keyword}, '%'))
    </select>

    <!-- ==================== 全局搜索 - 灵感 ==================== -->

    <!--
        功能: 搜索灵感记录（内容）
        参数: projectId - 项目ID, keyword - 搜索关键词
        返回: List<SearchResultDTO> 搜索结果列表
        说明:
            - 精确匹配(1)，模糊匹配(4)
            - type='inspiration', typeLabel='💡 灵感'
            - content同时作为title和description返回
    -->
    <select id="searchInspirations" resultType="com.novelcraft.web.model.SearchResultDTO">
        SELECT
          'inspiration' as type,
          '💡 灵感' as typeLabel,
          id,
          content as title,
          content as description,
          #{keyword} as keyword,
          CASE
            WHEN content = #{keyword} THEN 1
            WHEN content LIKE CONCAT('%', #{keyword}, '%') THEN 4
            ELSE 5
          END as priority,
          'content' as matchField,
          create_time as createTime,
          update_time as updateTime
        FROM novel_inspiration
        WHERE project_id = #{projectId}
          AND deleted = 0
          AND content LIKE CONCAT('%', #{keyword}, '%')
    </select>

    <!-- ==================== 全局搜索 - 伏笔 ==================== -->

    <!--
        功能: 搜索伏笔（名称、描述）
        参数: projectId - 项目ID, keyword - 搜索关键词
        返回: List<SearchResultDTO> 搜索结果列表
        说明:
            - 精确匹配名称优先级最高(1)，模糊名称(2)，模糊描述(4)
            - type='foreshadowing', typeLabel='🔗 伏笔'
    -->
    <select id="searchForeshadowings" resultType="com.novelcraft.web.model.SearchResultDTO">
        SELECT
          'foreshadowing' as type,
          '🔗 伏笔' as typeLabel,
          id,
          name as title,
          description,
          #{keyword} as keyword,
          CASE
            WHEN name = #{keyword} THEN 1
            WHEN name LIKE CONCAT('%', #{keyword}, '%') THEN 2
            WHEN description LIKE CONCAT('%', #{keyword}, '%') THEN 4
            ELSE 5
          END as priority,
          CASE
            WHEN name LIKE CONCAT('%', #{keyword}, '%') THEN 'name'
            WHEN description LIKE CONCAT('%', #{keyword}, '%') THEN 'description'
            ELSE 'description'
          END as matchField,
          create_time as createTime,
          update_time as updateTime
        FROM novel_foreshadowing
        WHERE project_id = #{projectId}
          AND deleted = 0
          AND (name LIKE CONCAT('%', #{keyword}, '%')
               OR description LIKE CONCAT('%', #{keyword}, '%'))
    </select>

</mapper>