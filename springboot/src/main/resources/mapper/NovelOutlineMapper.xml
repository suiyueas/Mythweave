<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.novelcraft.web.mapper.NovelOutlineMapper">

    <!-- 批量更新排序（同幕拖拽） -->
    <update id="updateBatchSort" parameterType="list">
        <foreach collection="items" item="item" separator=";">
            UPDATE novel_outline
            SET sort_order = #{item.sortOrder},
                node_number = #{item.nodeNumber},
                update_time = NOW()
            WHERE id = #{item.id}
              AND project_id = #{item.projectId}
              AND deleted = 0
        </foreach>
    </update>

    <!-- 批量更新排序和幕归属（跨幕拖拽） -->
    <update id="updateBatchSortAndAct" parameterType="list">
        <foreach collection="items" item="item" separator=";">
            UPDATE novel_outline
            SET sort_order = #{item.sortOrder},
                act = #{item.act},
                node_number = #{item.nodeNumber},
                update_time = NOW()
            WHERE id = #{item.id}
              AND project_id = #{item.projectId}
              AND deleted = 0
        </foreach>
    </update>

    <!-- 批量更新状态 -->
    <update id="updateBatchStatus">
        UPDATE novel_outline
        SET node_status = #{status},
            update_time = NOW()
        WHERE project_id = #{projectId}
          AND id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
          AND deleted = 0
    </update>

    <!-- 批量删除 -->
    <update id="deleteBatch">
        UPDATE novel_outline
        SET deleted = 1,
            update_time = NOW()
        WHERE project_id = #{projectId}
          AND id IN
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
          AND deleted = 0
    </update>

</mapper>
